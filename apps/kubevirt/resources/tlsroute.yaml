apiVersion: gateway.networking.k8s.io/v1alpha3
kind: TLSRoute
metadata:
  labels:
    cdi.kubevirt.io: cdi-uploadproxy
  name: cdi-uploadproxy
  namespace: cdi
spec:
  hostnames:
    - cdi-upload.engiqueersconcordia.ca
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: engiqueers-passthrough
      namespace: ingress
  rules:
    - backendRefs:
        - group: ''
          kind: Service
          name: cdi-uploadproxy
          port: 443
          weight: 1