namespace: website
helmCharts:
  - name: website
    version: 0.1.0
    releaseName: website
    namespace: website

helmGlobals:
  chartHome: local-charts

resources:
  - resources/redirect-route.yaml
  - resources/migration-route.yaml