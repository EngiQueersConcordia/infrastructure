helmCharts:
  - name: mc-router
    repo: https://itzg.github.io/minecraft-server-charts/
    version: 1.5.0
    releaseName: ingress-mc
    namespace: ingress
    valuesFile: values.yaml
resources:
  - resources/networkpolicy.yaml
patches:
  - target:
      kind: Deployment
      labelSelector: app.kubernetes.io/name=mc-router
    patch: |
      - op: replace
        path: /kind
        value: DaemonSet
      - op: remove
        path: /spec/replicas
      - op: remove
        path: /spec/strategy
    options:
      allowKindChange: true